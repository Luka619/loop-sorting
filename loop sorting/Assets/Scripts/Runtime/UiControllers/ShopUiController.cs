using UnityEngine;

namespace LoopSorting
{
    public sealed class ShopUiController
    {
        private readonly GameRuntimeController _host;

        public ShopUiController(GameRuntimeController host)
        {
            _host = host;
        }
    }
}
