using UnityEngine;

namespace LoopSorting
{
    public sealed class ResultUiController
    {
        private readonly GameRuntimeController _host;

        public ResultUiController(GameRuntimeController host)
        {
            _host = host;
        }
    }
}
